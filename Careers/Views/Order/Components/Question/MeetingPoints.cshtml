@model Question

<div class="jp_adp_form_wrapper">
    <select name="answer" id="points" asp-items="ViewBag.Points" data-placeholder="@(ViewBag.isRu ? Model.TextRU : Model.TextAZ)" multiple class="chosen-select"></select>
</div>

<script src="~/lib/chosen/chosen.jquery.min.js"></script>
<script>
    $('#points').chosen();
</script>