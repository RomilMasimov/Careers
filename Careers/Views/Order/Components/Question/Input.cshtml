@model Question

<div class="jp_adp_textarea_main_wrapper p-0 mt-4 border-0">
    <p>@(ViewBag.isRu ? Model.TextRU : Model.TextAZ)</p>
    <textarea name="answer" type="text" rows="3"></textarea>
</div>